/**
 * Time Tracking Tool Configurations
 * 
 * MCP tool definitions for work item time tracking functionality.
 * 
 * NOTE: Currently disabled - all tools need refactoring to work with new architecture
 * The tool implementations have been removed temporarily. They will be restored
 * once refactored to work with the current MCP server architecture.
 */

import type { ToolConfig } from '../../types/index.js';

/**
 * Export all time tracking tools
 * NOTE: Currently disabled - returns empty array until tools are refactored
 */
export const timeTrackingTools: ToolConfig[] = [];