/**
 * Test file to verify pre-commit hooks work correctly
 * This file is properly formatted and should pass all checks
 */

export function testFunction(input: string): string {
  return `Hello ${input}`;
}

export function anotherTest(x: number, y: number): number {
  return x + y;
}
